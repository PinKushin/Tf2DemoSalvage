# tests/

Golden-output regression tests: one fixture per demo in `tools/corpus/`, asserting the parser's structured output for that file doesn't silently change. Add a new fixture every time a new demo is added to the corpus.
